<?xml version='1.0' encoding='utf-8'?>
<command>
    <title>Consulta con JOIN y Agregación</title>
    <description>Une y agrega datos de ventas por fecha. [Source Table: FactSalesOrder; Joined Tables: DimDate]</description>
    <parts>
        <part>
            <text>SUM(so.SalesTotal) AS SalesRevenue</text>
            <desc>Calcula el total de ingresos.</desc>
        </part>
        <part>
            <text>JOIN DimDate AS d ON ...</text>
            <desc>Conecta las ventas con sus fechas correspondientes.</desc>
        </part>
    </parts>
    <full>SELECT d.[Year] AS CalendarYear, d.[Month] AS MonthOfYear, SUM(so.SalesTotal) AS SalesRevenue FROM FactSalesOrder AS so JOIN DimDate AS d ON so.SalesOrderDateKey = d.DateKey GROUP BY d.[Year], d.[Month], d.MonthName ORDER BY CalendarYear, MonthOfYear;</full>
</command>